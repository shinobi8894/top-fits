import HeaderContent from "./header-content";

export default function Header() {
    return (
        <header className="text-white w-full flex flex-row justify-center py-[50px] px-10">
            <HeaderContent />
        </header>
    );
}